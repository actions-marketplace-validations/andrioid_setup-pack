{
  "name": "setup-pack",
  "version": "1.0.0",
  "description": "something simple to setup and cache pack for github actions",
  "main": "index",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node12.x --outfile dist/index.js"
  },
  "author": "Andri Oskarsson",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.41",
    "esbuild": "^0.11.15",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@actions/core": "^1.2.7",
    "@actions/tool-cache": "^1.6.1"
  }
}
